apiVersion: v1
kind: PersistentVolume
metadata:
  name: static-web-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadOnlyMany
  hostPath:
    path: "C:/Users/beltr/web-content"  # ⚠️ Cambiá esto por la ruta real en tu PC
